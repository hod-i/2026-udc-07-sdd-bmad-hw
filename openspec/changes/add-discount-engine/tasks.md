# Tasks

Джерело поведінки — `docs/spec/pricing-discounts.md` (D-1…D-21, AC-1…AC-27).
Підхід до реалізації — `design.md`. Вимоги — `specs/order-pricing/discounts/spec.md`.

## 1. Каркас модуля

- [x] 1.1 Створити `app/src/discounts.ts` з типами `CouponRejectionReason`
      (шість причин), `RejectedCoupon` і `PriceBreakdown` за контрактом §5
      джерельної специфікації; перевірка: `npm run typecheck` без помилок
- [x] 1.2 Оголосити сигнатуру `priceOrder(order: Order, catalog: Coupon[], now: Date): PriceBreakdown`,
      імпортуючи типи з `./types.js` і функції з `./pricing.js`; перевірка:
      `npm run typecheck` проходить, `pricing.ts` і `types.ts` не змінені
      (`git diff --stat` порожній для них)
- [x] 1.3 Створити `app/src/discounts.test.ts` із фікстурами `item()` і `order()`
      у стилі наявного `pricing.test.ts`; перевірка: `npm test` зелений, 8
      засіяних тестів проходять

## 2. Рівень лояльності та кістяк розрахунку

- [x] 2.1 Реалізувати кроки 1–3 нормативного алгоритму (`subtotal`,
      `tierDiscount` із `floor` та обрізанням, біжуча `base`); перевірка: тести
      `AC-1:` і `AC-13:` проходять
- [x] 2.2 Реалізувати крок 5 (`total = base + shippingKopecks`) і повернення
      розбивки без обробки купонів; перевірка: тести `AC-14:` і `AC-17:`
      проходять — знижка не чіпає доставку, рівень діє на всі категорії

## 3. Нормалізація та пошук у каталозі

- [x] 3.1 Реалізувати нормалізацію `trim().toUpperCase()` для введеного коду й
      для коду каталогу (обидві сторони, D-11/D-21); перевірка: тести `AC-23:` і
      перша половина `AC-27:` проходять
- [x] 3.2 Реалізувати пошук у каталозі з трьома результатами: нема збігів →
      `unknown`, один збіг → купон, більше одного → `invalid` (D-10, D-21);
      перевірка: тести `AC-8:` і друга половина `AC-27:` проходять
- [x] 3.3 Реалізувати дедуплікацію по **всіх побачених** нормалізованих кодах
      (D-12); перевірка: тести `AC-9:` і `AC-21:` проходять

## 4. Валідація запису каталогу

- [x] 4.1 Реалізувати перевірку `value` через дозволене
      (`Number.isInteger(value) && value >= 0`, для `percent` ще `<= 100`) —
      D-16; перевірка: тести `AC-12:`, `AC-18:`, `AC-26:` проходять і жодне поле
      не стає `NaN`
- [x] 4.2 Реалізувати перевірку однозначності `expiresAt` (форма «тільки дата»
      або форма зі зсувом; усе інше — `invalid`) — D-20; перевірка: тести
      `AC-24:` і `AC-25:` проходять
- [x] 4.3 Реалізувати перевірку `minSubtotalKopecks` на ціле невідʼємне, якщо
      поле присутнє (D-20); перевірка: `npm run typecheck` без помилок і тест
      `AC-11:` не зачеплений регресією

## 5. Перевірки придатності купона

- [x] 5.1 Реалізувати перевірку строку дії `now >= expiresAt` **після**
      перевірки валідності (D-8, D-9); перевірка: тест `AC-5:` проходить —
      межа «на або після» спрацьовує рівно в момент закінчення
- [x] 5.2 Реалізувати перевірку порога проти **вихідного** `subtotal` (D-6);
      перевірка: тести `AC-10:` і `AC-11:` проходять
- [x] 5.3 Реалізувати перевірку відсутньої категорії (D-19); перевірка: тести
      `AC-20:` і `AC-21:` проходять
- [x] 5.4 Звірити, що всі перевірки виконуються ланцюгом із раннім виходом у
      порядку (б)…(є) з нормативного алгоритму; перевірка: тест `AC-21:`
      проходить (перше входження — `category_absent`, друге — `duplicate`)

## 6. Арифметика знижок

- [x] 6.1 Реалізувати базу купона: некатегорійний — біжуча `base`; категорійний —
      `floor(categorySubtotal × base / subtotal)` з `couponBase = 0` при
      `subtotal === 0` (D-5); перевірка: тести `AC-3:` і `AC-15:` проходять
- [x] 6.2 Реалізувати обчислення знижки: `percent` — `floor(couponBase × value / 100)`,
      `fixed` — `min(value, couponBase)` (D-13); перевірка: тести `AC-2:`,
      `AC-7:`, `AC-16:` проходять
- [x] 6.3 Реалізувати друге обрізання до біжучої `base`, віднімання від бази й
      накопичення `couponDiscount` (D-7); перевірка: тести `AC-4:` і `AC-6:`
      проходять — підсумок не відʼємний, порожнє замовлення не кидає винятку
- [x] 6.4 Реалізувати правило вичерпаної бази: купон із нульовою знижкою
      потрапляє в `appliedCoupons` (D-18); перевірка: тест `AC-19:` проходить
- [x] 6.5 Звірити, що перевпорядкування купонів ніде не відбувається (D-14);
      перевірка: тест `AC-22:` проходить — обидва порядки дають різні суми

## 7. Інваріанти контракту

- [x] 7.1 Додати спільний хелпер перевірки шести інваріантів §5 і застосувати
      його до результату **кожного** тесту за AC; перевірка: `npm test` зелений,
      жоден інваріант не порушено на жодній фікстурі
- [x] 7.2 Перевірити чистоту функції: `order` і `catalog` не мутуються, годинник
      не читається; перевірка: тест порівнює глибокі копії входу до і після
      виклику та проходить

## 8. Трасування й приймання

- [x] 8.1 Заповнити `docs/traceability.md`: рядок на кожен AC-1…AC-27 із
      посиланням на реальний код і реальний тест; перевірка: порожніх клітинок
      немає
- [x] 8.2 Заповнити зворотну перевірку в `docs/traceability.md` (код без AC /
      AC без тесту / тест без AC); перевірка: усі три розділи заповнені, а не
      позначені як «немає» без розбору
- [x] 8.3 Фінальне приймання: `cd app && npm test` зелений (8 засіяних плюс усі
      тести за AC) і `npm run typecheck` без помилок; перевірка: обидві команди
      завершуються з нульовим кодом
